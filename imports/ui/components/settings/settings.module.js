'use strict';

import angular from 'angular';
import angularMeteor from 'angular-meteor';
import uiRouter from 'angular-ui-router';


/**
 * @ngdoc module
 * @name kaiamSettings
 * @description
 *
 * The `kaiamSettings` module contains UI for all the settings in application
 */
angular.module('kaiamSettings', [
    angularMeteor,
    uiRouter
]);

